# 📘 Лабораторные работы по C++

Данный репозиторий содержит 4 лабораторные работы по языку программирования C++, каждая из которых размещена в отдельной директории:

```
LizaLabs/1Lab/
LizaLabs/2Lab/
LizaLabs/3Lab/
LizaLabs/4Lab/
```

В каждой папке находятся:
- 💻 исходный файл программы (`Xlab.cpp`)
- 📄 отчёт в формате `.docx`
- 📦 дополнительные файлы (в 4Lab: `data.txt`, `json.hpp`, `result.json`)

---

## 🚀 Как запустить программу

Все команды приведены для **Linux / macOS**.  
На Windows рекомендуется использовать **MinGW**, **WSL**, либо терминал MSYS2.

---

## 📂 1. Переход в папку лабораторной работы

Откройте терминал и перейдите в нужную директорию:

```bash
cd Путь/к/репозиторию/1Lab
```

Например:

```bash
cd ~/Projects/CppLabs/1Lab
```

---

## ⚙️ 2. Компиляция программы

Для каждой лабораторной используется один основной `.cpp`-файл.

Примеры компиляции:

```bash
g++ 1lab.cpp -o 1lab
g++ 2lab.cpp -o 2lab
g++ 3lab.cpp -o 3lab
g++ 4lab.cpp -o 4lab
```

---

## ▶️ 3. Запуск программы

После успешной компиляции выполните:

```bash
./1lab
```

Для остальных:

```bash
./2lab
./3lab
./4lab
```

---

## 📑 Структура проекта

```
1Lab/
 ├── 1lab.cpp
 └── Отчёт 1 Практическая.docx

2Lab/
 ├── 2lab.cpp
 └── Отчёт 2 Практическая.docx

3Lab/
 ├── 3lab.cpp
 └── Отчёт 3 Практическая.docx

4Lab/
 ├── 4lab.cpp
 ├── data.txt
 ├── json.hpp
 ├── result.json
 └── Отчёт 4 Практическая.docx
```

---

## 🛠 Требования

- Компилятор **g++**

  Проверка версии:

  ```bash
  g++ --version
  ```

- Для 4-й лабораторной используется библиотека `json.hpp` (уже включена в репозиторий)
